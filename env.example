# Database connection string for PostgreSQL on Vercel
DATABASE_URL="postgresql://username:password@hostname:port/database?schema=public"

# Session secret (replace this with a random string in production)
SESSION_SECRET="tianxia-taiping-production-secret-key"

# Node environment
NODE_ENV="production" 